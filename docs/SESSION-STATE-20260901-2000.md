# Session state, 2026-09-01 ~20:00 EDT — READ BEFORE TOUCHING THE HOST

🚨 **THE HOST IS IN A HALF-DEPLOYED STATE. This is the single most important
fact in this file.**

## 1. Host state

| | |
| --- | --- |
| Files on `/config/custom_components/mammotion` | **beta96** (48/48 byte-identical, verified) |
| Card at both serving paths | **beta96**, md5 `086a7d24` |
| **Running Home Assistant process** | **still beta95 — HA was NEVER RESTARTED** |
| Lovelace resource key | **still beta95's** — not bumped |
| Backend | `chorty-0.8.12.post4` |
| Motion gate | **disarmed**, verified live API + RAW after every dispatch |
| Mower | **on the dock**, charging, was 40% at docking |

🔑 **So beta96 is staged, not live.** The restart was deliberately interrupted to
run an adversarial review first. ⚠️ **If HA restarts for ANY reason — crash,
update, power — it will load beta96 without the review having been completed.**

**To finish the deploy** (`docs/deploy-runbook-p0.md` steps 5-7):
`scripts/ha_restart.sh`, then
`scripts/ha_set_card_resource.py "0.6.4-beta96&build=086a7d24" --apply`, then the
verification tail, then a browser check of the card footer.

**To back out instead:** `/config/mammotion-backup-20260901-1917-pre-beta96.tgz`.

## 2. What beta96 contains

`step_ms` 7000 -> 15000, `_STEP_RESPONSE_MAX_TOTAL_MS` 16000 -> 23000,
`linear_speed` `[400]` -> `[300, 400]`, and a new pre-dispatch blocker
`step_window_travel_exceeds_budget`. **`max_travel_m` is UNCHANGED at 4.5** —
this raises the clock, not the distance. Predeclared in
`docs/phase2-long-step-slow-speed-predeclared-20260901.md`, **including its §6
corrections — read those, they materially weaken the expected result.**

## 3. Today's measurement result

The +180/7000 repeat **FAILED 2a** under E-VIO (`half_diff` 3.4049 vs the 1.5
bound), where the banked run scored 0.130. **The banked flip does not reproduce.**
🗑️ **`tau = 0.80 s` is not quotable.** Offline comparison then showed the two runs
agree on the plant to **0.195 °/s** and the entire split came from the single
onset-contaminated interval — a sampling-phase artifact, not the mower. Evidence:
`docs/evidence-route1-plus180-repeat-2a-fail-20260901.md`,
`docs/findings-plus180-split-is-onset-sampling-phase-20260901.md`.

## 4. Open, in priority order

1. **Finish or back out the beta96 deploy.** Do not leave the host half-deployed.
2. **The adversarial review of beta96 did not complete.** A first run lost all 9
   agents to a session limit (⚠️ its empty result meant "nothing ran", NOT
   "clean"); a second 4-dimension run was still in flight at session end. Partial
   findings are in this session's workflow journal under
   `subagents/workflows/wf_16a690e1-718/`. **Re-run before the code drives the
   mower.** Dimensions not yet covered at all: E-VIO scoring at larger n,
   travel-guard integrity over a longer window, schema/YAML consistency,
   regression risk.
3. **No sustained-speed measurement exists at linear 300.** Everything about the
   intended run's travel is extrapolated. A short characterisation pulse at 300
   would de-risk it cheaply.
4. 🔐 **`HA_SSH_PASS` rotation still unconfirmed** on the host and in `.env`.

## 5. Standing boundaries, unchanged

🛑 Phase 2 continuous steering stays **PARKED** (standing decision 5). beta96
repairs a parked instrument; it does not resume the work. Any run needs explicit
per-run authorization, daylight throughout (dark VIO ⇒ `vio_not_live_throughout`,
unscoreable by design), a docked-and-charged battery, a fresh corridor scan, and
the gate disarmed and verified from live API **and** RAW afterwards.

---

## 6. UPDATE — the staged beta96 is now KNOWN-DEFECTIVE. Roll it back.

The adversarial review completed (partially — see §7) and found real defects in
beta96. **The bytes sitting on `/config` contain them.** The tree has since moved
past beta96, so the staged build is both defective and stale.

🔑 **Recommendation: roll the host back to beta95** —
`/config/mammotion-backup-20260901-1917-pre-beta96.tgz`, then restart. That
returns the host to the last fully-verified, browser-confirmed build and removes
the "if HA restarts it loads unreviewed defective code" hazard. **Nothing is lost
by doing so**: beta96 existed to enable a run that §7 says should not be
dispatched as designed.

**Do NOT finish the beta96 deploy.**

### What was wrong with beta96 (fixed in the tree, commit `29b810bf`)

1. **`_STEP_RESPONSE_MIN_SPEED_BY_LINEAR[400] = 0.24` was not a lower bound.**
   Cumulative path travel over every banked linear-400 run:
   `0.1936 0.2000 0.2121 0.2124 0.2616` m/s. 0.24 was taken from the single
   FASTEST run while its comment called it the slowest, so it **over-refused** —
   it would refuse a replay of banked route-1 run 1 at `max_travel_m` 3.0, a
   config the mower has already completed twice at 2.71 m and 2.77 m. Now 0.17.
2. **`strings.json` / `en.json` still said "Fixed at the measured value 400".**
   HA resolves service field descriptions from translations **in preference to**
   `services.yaml`, so every word of beta96's new prose was unreachable and the
   UI stated the opposite of what the schema accepts.

## 7. 🚨 The headline finding: the intended run should NOT be dispatched as designed

**`half_diff ≈ |steady − onset| / k` models ONLY the onset interval and omits the
residual scatter.** Removing the onset interval entirely from the banked +180 run
still leaves `half_diff = 1.625 °/s` — over the 1.5 bound. **So it does not tend
to zero as k grows**, and §6.2's "passes by 8.1%" is the onset-only figure.

Bootstrapping the real ex-onset scatter (mean −12.14, sd 1.383 °/s):

| step length | k | P(2a pass) |
| --- | --- | --- |
| 15 s (the beta96 config) | 8 | **58.5%** |
| 14 intervals | 7 | 47.7% |
| 20 s | 10 | 76% |

🔑 **The predeclared run is a ~59/41 coin flip on residual noise alone** — the
same trap as the +180 repeat, re-weighted. A FAIL would be recorded as "the plant
is not steady" when it is the statistic's own noise floor; a PASS is equally
uninformative. ⚠️ **Even a 20 s step only reaches 76%**, so "make the step longer"
is not a fix, it is a slightly better coin.

**The honest conclusion: 2a needs a different statistic, not a longer window.**
That is offline work and needs no mower.

⚠️ **This finding is UNVERIFIED** — every one of its adversarial verifiers died on
a spend limit before returning. Its internal evidence (re-scoring all five banked
runs through the shipped code, plus the bootstrap) is strong but has not been
independently challenged. **Re-verify before acting on it as settled.**

Also unverified, same cause: **(linear 300, angular ±180) is an operating point
that has never been exercised** — `grep '"linear_speed": 300'` finds nothing in
`docs/`. The 10.43 °/s onset figure was measured at linear 400 and may not
transfer, which would undercut the `k >= 7` derivation at its root.

### Review coverage, stated honestly

| dimension | status |
| --- | --- |
| travel-gate-logic | reviewed AND verified |
| linear-300-plumbing | findings produced, **verifiers all died — unverified** |
| arithmetic-soundness | findings produced, **verifiers all died — unverified** |
| long-window-downstream | **never ran** |
| E-VIO scoring at larger n | **never ran** |
| travel-guard integrity | **never ran** |
| schema/YAML consistency | **never ran** |
| regression risk | **never ran** |

An earlier full run lost all 9 agents to a session limit and returned
`confirmed: []` — ⚠️ **that empty result meant "nothing ran", not "clean".**
