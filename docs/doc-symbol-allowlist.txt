# Names the session-entry docs may cite even though they are not in the tree.
#
# Checked by `scripts/check_doc_symbols.py`. Every entry needs a reason, so a
# symbol deleted from the code forces a deliberate note here instead of leaving
# a silently false claim in CLAUDE.md / NEXT-SESSION.md / CODEX-HANDOFF.md.
#
# Seeded 2026-08-14 from the first full audit (989 claims checked).
#
# An entry here means "the doc may NAME this, because it now says correctly what
# happened to it" -- not "ignore this". The two genuine defects found that day
# were fixed by annotating the claim in place, which necessarily keeps the old
# name in the prose, so they are listed like any other rename.

# --- renamed or removed, where the doc now records what happened ---
_VIO_TURN_MODES                                 # renamed -> _SEGMENT_TURN_MODES; value gained "night" (night v1). Annotated at all 4 mention sites 2026-08-14
_VIO_TURN_CONSERVATIVE_MAX_DEGREES_PER_SECOND   # removed in beta37; the beta31/32 sections that discuss it are annotated as history
per_command_translation_bound_m                 # renamed -> translation_bound_m_per_degree (beta20)
per_command_translation                         # prose fragment of the same rename note
_VIO_TURN_CONSERVATIVE_TRANSLATION_M_PER_SECOND # replaced by _..._M_PER_DEGREE (beta20)

# --- options considered and rejected; the doc records why ---
rtk_not_fixed                                   # rejected hard-blocker name; shipped as rtk_not_precise
arc_to_point                                    # hypothetical controller, never built
sync_rtk_and_dock                               # rover-side action tried 2026-08-07; no such symbol exists

# --- fields the docs correctly describe as absent or dead ---
rtk_signal_quality                              # named as a dead/unpopulated field

# --- exists UPSTREAM only; deliberately not in our pinned backend ---
last_report_data_at                             # upstream pymammotion c21ec18; absent from chorty-0.8.12.post1. Cited in CLAUDE.md as the instrument that would close the beta23 cadence gap IF the sensing floor is ever reconsidered
transport/ble.py                                # a pymammotion path, not one of ours: the file our fork patches for the BLE leak, and the one upstream 47c3c54 also touches

# --- paths and names on the HA host, not in this repo ---
www/mammotion-custom-path-card.js               # the /config/www/community copy; the repo has only custom_components/mammotion/www/
dashboard_yard                                  # Lovelace dashboard name in /config/.storage backup filenames

# --- external systems: ESPHome, Home Assistant core, tooling ---
esp32_ble_tracker                               # ESPHome config key
bluetooth_proxy                                 # ESPHome config key
setup_error                                     # Home Assistant ConfigEntryState
_fix_tokens                                     # referenced in a .pre-commit-config.yaml comment

# --- math notation inside formulas, not code identifiers ---
initial_aim                                     # variable in the landing-error fit
map_facing                                      # variable in a heading derivation
toward_first                                    # variable in the --summarise offset formula

# --- keys that live only in a docs/evidence-*.json record ---
CORRECTION_read_this_first                      # a JSON key inside docs/evidence-routeb-retry-overshoot-20260820.json, holding the 2026-08-21 correction to commit 845ddf3e. The checker scans code trees only, so an evidence-file key can never resolve

# --- Home Assistant entity ids on the host, not code identifiers ---
mammotion_disarm_motion_gate_when_left_armed    # the disarm automation's entity id, created from docs/automations/disarm-motion-gate.yaml in the host's /config/automations.yaml on 2026-08-22. Host config, so it can never resolve in this tree
back_yard_clip_skywalker_real_motion_ready      # the binary_sensor entity id that automation triggers on. Built at runtime from the device name plus the `real_motion_ready` key in binary_sensor.py, so the full id exists only on the host
armed_but_blocked                               # a trigger id inside docs/automations/disarm-motion-gate.yaml and the host's /config/automations.yaml. Automation YAML, not code, so the checker's code trees can never resolve it

# --- names deliberately cited as the OLD form of a rename ---
background_quiesced_at_monotonic                 # the pre-post3 lease field, renamed to background_stop_enqueued_at_monotonic on 2026-08-25 because it asserted a quiescence the code never established. CLAUDE.md names the old form so a reader of an older evidence artifact can find the change; by design it no longer exists in the tree
include_raw_samples                              # the sequence-probe flag that stripped per-sample position intervals from matrix artifacts while keeping generic report intervals. Removed 2026-08-26 after it made the beta77 matrix less auditable than beta76's; CLAUDE.md names it so a reader of that artifact knows why its raw position evidence is missing

# --- names that live only in scripts/ha_ssh.exp, a .exp file the checker's scan patterns don't cover ---
HA_SSH_PASS                                      # the env var scripts/ha_ssh.exp reads for the HA host's SSH password; the checker scans .py/.js trees only, so a real .exp symbol can never resolve
exp_continue                                     # the Tcl/expect builtin used in scripts/ha_ssh.exp's password-prompt loop; same .exp scan-coverage gap as HA_SSH_PASS above

# --- paths that live in the sibling Chorty/PyMammotion fork repo, not this tree ---
scripts/update_credentials.py                    # upstream PyMammotion's build-time credential injector, cited by CLAUDE.md's 2026-08-31 login root-cause entry. It exists in the PyMammotion fork checkout, never in Mammotion-HA
# Home Assistant entity ids that live on the host, not in this tree. Recorded
# 2026-09-01 after the master_bedroom_proxy outage blocked an authorized run;
# they are the discriminator for "BLE cannot connect", so the prose needs them.
bermuda_global_active_proxy_count
master_bedroom_proxy_
master_bedroom_proxy

# --- checker false positives from regex literals in code spans ---
Z_                                               # not a symbol -- a fragment of the `[A-Z_]*=`/`[A-Z_]+=` regex character classes in CLAUDE.md's 2026-09-04 credential-hygiene note (grep patterns for env var names), mis-tokenized as an identifier

# --- Home Assistant core fields, not defined in this integration's tree ---
disabled_by                                      # a field on HA core's own ConfigEntry object (homeassistant.config_entries), read from the host's raw core.config_entries storage in the beta101 deploy-runbook entry; this repo only consumes it, never defines it
