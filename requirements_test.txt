# Test/lint toolchain. pytest-homeassistant-custom-component hard-pins both
# homeassistant and pytest, so those three must move together.
homeassistant==2026.7.1
pytest==9.0.3
pytest-cov==7.1.0
pytest-homeassistant-custom-component==0.13.345
ruff==0.15.16
mypy==2.1.0

# The integration's own runtime requirements. Keep the pymammotion pin
# byte-identical to custom_components/mammotion/manifest.json: the backend
# capability probes assert against whatever is installed, so a drift here would
# test a different backend than the one shipped.
pymammotion@https://github.com/Chorty/PyMammotion/releases/download/chorty-0.8.12.post3/pymammotion-0.8.12.post3-py3-none-any.whl
websockets==16.0
sdp-transform==1.1.0
autotyping==24.9.0

# NOTE: the HA *component* requirements this integration depends on
# (bluetooth_adapters and friends) are deliberately NOT listed here. They are
# resolved from the installed HA manifests by the "Install HA component
# requirements" step in .github/workflows/validate.yml, so they track whatever
# homeassistant version is pinned above instead of drifting out of date.
